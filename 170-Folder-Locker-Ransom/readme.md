# 🔒 Folder Locker (Ransomware Logic)

## Description
Simulates ransomware behavior. It scans a specific folder (`test_folder`), encrypts all files using AES encryption, appends a `.locked` extension, and deletes the originals.

## WARNING
**DO NOT RUN** on important folders. Without the generated `MASTER_KEY.key`, the files are unrecoverable.