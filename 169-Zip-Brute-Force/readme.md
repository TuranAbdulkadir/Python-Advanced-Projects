# 🔨 Zip Brute Force

## Description
Performs a dictionary attack against a password-protected ZIP file. It iterates through a list of common passwords to find the correct one.

## Usage
1. Place a password-protected zip file named `gizli.zip` in the folder.
2. Edit the `wordlist` array to add more password guesses.