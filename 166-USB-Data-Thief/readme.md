# 💾 USB Data Thief

## Description
A "Dead Drop" tool. It runs silently in the background and waits for a USB drive to be inserted. Once detected, it automatically copies documents and images from the USB to a hidden folder on the computer.

## Usage
Run the script and insert a flash drive containing files.