# 🕒 Scheduled Task Persistence

## Description
Demonstrates how malware survives reboots. Instead of using the common Registry keys, this script creates a Windows Scheduled Task that runs with highest privileges every time the user logs on.

## Usage
Must be run as **Administrator**.